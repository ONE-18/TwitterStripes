services:
  app:
    build: .
    ports:
      - "20813:3000"
    volumes:
      - ./output:/app/output
      - ./public:/app/public
    environment:
      - NODE_ENV=production
    restart: unless-stopped
